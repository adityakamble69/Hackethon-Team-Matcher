Drop your background video here as: hero.mp4
(i.e. client/static/assets/hero.mp4)

VideoBg.svelte already points at /assets/hero.mp4. Until the file exists,
the landing page falls back to a plain dark gradient background instead of
a broken video tag — nothing to configure, just add the file.
